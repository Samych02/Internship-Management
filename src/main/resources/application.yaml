server:
  port: 8080

spring:
  application:
    name: Internship-Management-Backend

  datasource:
    url: jdbc:postgresql://localhost:5432/Internship-Management
    username: admin
    password: admin

  jpa:
    properties:
      hibernate:
        format_sql: false
        show_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

  servlet:
    multipart:
      enabled: true
      max-file-size: 15MB
      max-request-size: 15MB

resources-directory: C:\Users\scherif\OneDrive - Capgemini\InternshipManagementResources
